@model ProductViewModel

@{
    ViewData["Title"] = "Детали товара";
}

<h2 class="mb-3">Детали товара</h2>

<table class="table table-bordered">
    <tr>
        <th>Название</th>
        <td>@Model.Name</td>
    </tr>
    <tr>
        <th>Описание</th>
        <td>@Model.Description</td>
    </tr>
    <tr>
        <th>Категория</th>
        <td>@Model.CategoryName</td>
    </tr>
    <tr>
        <th>Цена</th>
        <td>@Model.Price.ToString("0.00")</td>
    </tr>
    <tr>
        <th>Остаток</th>
        <td>@Model.Quantity</td>
    </tr>
</table>

@if (User.Identity != null && User.Identity.IsAuthenticated && !User.IsInRole("Администратор"))
{
    <form asp-action="AddToCart" method="post" class="mb-3">
        @Html.AntiForgeryToken()

        <input type="hidden" name="productId" value="@Model.Id" />

        <div class="mb-3">
            <label class="form-label">Количество</label>
            <input type="number" name="quantity" value="1" min="1" max="@Model.Quantity" class="form-control" style="max-width: 200px;" />
        </div>

        <button type="submit" class="btn btn-success" @(Model.Quantity <= 0 ? "disabled" : "")>
            Добавить в корзину
        </button>
    </form>
}

@if (User.IsInRole("Администратор"))
{
    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning">Изменить</a>
}

<a asp-action="Index" class="btn btn-secondary">Назад</a>
